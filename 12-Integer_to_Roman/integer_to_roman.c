#include <stdio.h>
#include <string.h>

char *intToRoman(int num) {
	
}

int main() {
	int input;

	printf("Enter the number : %d", input);
	scanf("%s", input);

	char* output = intToRoman(input);
	return 0;
}